# Install Yowl as a PWA from Android Chrome, then confirm the standalone app.
# Prefer APP_URL=http://localhost:<port> via adb reverse (secure context).
# Uses clearState so a prior "Dismiss install prompt" does not hide the banner.
#
#   yarn test:maestro:android:pwa
#
appId: ${APP_ID}
androidWebViewHierarchy: devtools
---
- runFlow:
    file: android-chrome-open.yaml
- runFlow:
    when:
      visible: "Close"
    commands:
      - tapOn: "Close"
# Keep install banner — do not dismiss it.
- extendedWaitUntil:
    visible: ".*Sign in to Yowl|Your rooms.*"
    timeout: 20000
- runFlow:
    when:
      visible: "Sign in to Yowl"
    file: shared/login-steps.yaml
- extendedWaitUntil:
    visible: "Your rooms"
    timeout: 20000
- extendedWaitUntil:
    visible: "Install Yowl"
    timeout: 45000
- tapOn: "Install app"
- waitForAnimationToEnd
- extendedWaitUntil:
    visible: "Install"
    timeout: 15000
- tapOn: "Install"
- waitForAnimationToEnd
- runFlow:
    when:
      visible: "Add to home screen"
    commands:
      - tapOn: "Add to home screen"
      - waitForAnimationToEnd
- runFlow:
    when:
      visible: "Add"
    commands:
      - tapOn: "Add"
      - waitForAnimationToEnd
- pressKey: Home
- waitForAnimationToEnd
- runFlow:
    when:
      notVisible: "Yowl"
    commands:
      - swipe:
          start: 50%, 90%
          end: 50%, 30%
      - waitForAnimationToEnd
- runFlow:
    when:
      notVisible: "Yowl"
    commands:
      - swipe:
          direction: LEFT
- runFlow:
    when:
      notVisible: "Yowl"
    commands:
      - swipe:
          direction: LEFT
- extendedWaitUntil:
    visible: "Yowl"
    timeout: 20000
- tapOn: "Yowl"
- extendedWaitUntil:
    visible: ".*Sign in to Yowl|Your rooms.*"
    timeout: 45000
- runFlow:
    when:
      visible: "Sign in to Yowl"
    file: shared/login-steps.yaml
- assertVisible: "Your rooms"
